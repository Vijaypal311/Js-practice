"use strict" // treat all js code as newer version

// alert(3 + 3); // we are using nodejs not browser.

// DataTypes in JavaScript

// Number
// BigInt
// String
// Boolean -> true/false
// null -> StandAlone Value
// undefined
// symbol => unique



console.log(typeof "Vijay"); // typeof returns the datatype.
console.log(typeof null); // typeof null returns objects many says that it is bug in the language.
